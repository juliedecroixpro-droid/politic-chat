# Frontend Public Chat - Production Environment Variables
# Set these in Vercel Dashboard

# API Backend URL (will use Vercel proxy /api/* → Railway)
# No need to set VITE_API_URL since we use relative /api paths
